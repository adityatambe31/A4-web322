<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Climate Solutions</title>
    <link rel="stylesheet" href="/css/main.css" />
</head>
<body tabindex="-1">
    <%- include('partials/navbar', { page: "/projects" }) %>
        <br />
        <div class="container mx-auto">
            <!-- Hero Section -->
            <div class="container mx-auto">
                <div class="hero bg-base-200 rounded-lg p-6">
                    <div class="hero-content text-center ">
                        <div class="max-w-md">
                            <h1 class="text-5xl font-bold">Projects</h1>
                            <p class="py-6">Browse the current list of Projects in our database. Click the theme to
                                filter by sectors such as:</p>
                            <a href="/solutions/projects?sector=ind" class="btn btn-primary">Industry</a>
                            <a href="/solutions/projects?sector=transportation"
                                class="btn btn-primary">Transportation</a>
                            <a href="/solutions/projects?sector=electricity" class="btn btn-primary">Electricity</a>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Project List -->
            <table class="table table-zebra">
                <tbody>
                    <% if (projects && projects.length> 0) { %>
                        <% projects.forEach(project=> {%>
                            <tr>
                                <td>
                                    <span class="font-bold">
                                        <%=project.title%>
                                    </span>
                                </td>
                                <td>
                                    <a href="/solutions/projects?sector=<%=project.sector%>" class="btn mt-2 btn-xs">
                                        <%=project.sector%>
                                    </a>
                                </td>
                                <td>
                                    <span>
                                        <%=project.summary_short%>
                                    </span>
                                </td>
                                <td>
                                    <a href="/solutions/projects/<%=project.id%>" class="btn btn-xs">details</a>
                                </td>
                            </tr>
                            <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="4" class="text-center">No projects found.</td>
                                    </tr>
                                    <% } %>
                </tbody>
            </table>
</body>

</html>